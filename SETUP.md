1. npm install

2. create a postgress locally

3. copy the .env.example to .env ( new file you must create) in the root

4. update the DATABASE_URL in .env with your database url

5. npm build

5. npm migrate:up

7. npm run dev

and that it go to your localhost. I might be missing something but that is rough setup you need